# DB (Compose)
POSTGRES_DB=costs
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
DATABASE_URL=postgresql+psycopg2://postgres:postgres@db:5432/costs

# App
DEMO_MODE=1
TZ=Europe/Lisbon

# Slack (rotate your webhook; leave blank if not ready)
SLACK_WEBHOOK_URL=

# Auth (same value used by frontend)
API_DEMO_TOKEN=2025-cloud-cost-copilot-demo-token

# Frontend proxy target (inside Docker use the service name)
NEXT_PUBLIC_API_BASE=http://backend:8000
# Frontend sends the token in Authorization header
NEXT_PUBLIC_API_TOKEN=2025-cloud-cost-copilot-demo-token
